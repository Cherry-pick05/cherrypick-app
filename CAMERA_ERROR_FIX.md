# 카메라 이미지 변환 오류 해결 가이드

## 🔍 문제

```
카메라 이미지 변환 오류: RangeError (length): Invalid value: Only valid value is 0: 1
```

이 오류는 카메라 스트림에서 실시간으로 이미지를 변환할 때 발생합니다.

---

## ✅ 해결 방법

### 방법 1: 실시간 탐지 비활성화 (현재 적용됨)

실시간 스트림 탐지를 비활성화하고, **촬영 시에만 탐지**하도록 변경했습니다.

**장점**:
- ✅ 오류 없음
- ✅ 촬영 시 탐지는 정상 작동
- ✅ 더 안정적

**단점**:
- ⚠️ 실시간 탐지 표시 불가

### 방법 2: 카메라 이미지 변환 로직 수정 (고급)

실시간 탐지를 원하는 경우, 카메라 이미지 변환 로직을 더 안전하게 수정:

```dart
// _cameraImageToBytes 함수에서 더 안전한 접근
// planes 배열 길이 확인 강화
// 인덱스 범위 체크 강화
```

---

## 📋 현재 동작 방식

1. **카메라 열기**: 카메라 프리뷰 표시
2. **촬영하기 버튼 클릭**: 
   - `takePicture()`로 이미지 캡처
   - 캡처된 이미지로 탐지 수행
   - 결과 표시

3. **실시간 탐지**: 비활성화됨 (오류 방지)

---

## 🧪 테스트

### 현재 상태

1. ✅ 카메라 열림
2. ✅ 촬영하기 버튼 작동
3. ✅ 촬영 후 탐지 작동
4. ⚠️ 실시간 탐지 비활성화

### 확인할 사항

촬영하기 버튼을 눌렀을 때:
- 디버그 콘솔에 다음 로그가 보여야 함:
  ```
  📸 사진 촬영 시작...
  ✅ 사진 촬영 완료: X bytes
  🔍 YOLO World 탐지 시작...
  ✅ 탐지 완료: X개 객체 발견
    탐지된 객체: ...
  ```

---

## 🔧 실시간 탐지 재활성화 (선택사항)

실시간 탐지를 다시 활성화하려면:

```dart
// camera_detector_view.dart의 _startImageStream() 함수에서
// 주석 처리된 부분을 활성화하고 카메라 이미지 변환 로직 수정 필요
```

---

## 💡 디버깅 팁

### 1. 촬영 시 탐지 확인

디버그 콘솔 확인:
- `📸 사진 촬영 시작...` → 촬영 시작
- `✅ 사진 촬영 완료` → 이미지 캡처 성공
- `🔍 YOLO World 탐지 시작...` → 탐지 시작
- `✅ 탐지 완료: X개` → 탐지 성공

### 2. 탐지가 안 될 때

1. **임계값 낮추기**:
   ```dart
   threshold: 0.1,  // 0.25 → 0.1
   ```

2. **출력 로그 확인**:
   - 탐지 완료 후 라벨이 출력되는지 확인
   - `🔍 추론 출력 통계` 확인

---

## 📝 요약

- ✅ **촬영 시 탐지**: 정상 작동
- ⚠️ **실시간 탐지**: 비활성화됨 (오류 방지)
- 🔧 **다음 단계**: 촬영 시 탐지가 정상 작동하는지 확인

이제 앱을 실행하고 촬영하기 버튼을 눌러 테스트해보세요!

